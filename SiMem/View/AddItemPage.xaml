<ContentDialog
    x:Class="SiMem.View.AddItemPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SiMem.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="ADD MEMORY"
    PrimaryButtonText="save"  
    SecondaryButtonText="cancel"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick">

    <StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch" x:Name="rootLayout" >
        <TextBox x:Name="titleText" Header="title"/>
        <ScrollViewer x:Name="textScrollView" VerticalScrollBarVisibility="Auto">
            <TextBox x:Name="textText" Header="content" MaxHeight="200" TextWrapping="Wrap"
        	GotFocus="textText_GotFocus" />
        </ScrollViewer>
    </StackPanel>
</ContentDialog>
